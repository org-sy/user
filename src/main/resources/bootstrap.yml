eureka:
  client:
    service-url:
      defaultZone: http://47.101.168.240:8761/eureka
server:
  port: 8083
spring:
  application:
    name: user
  servlet:
    multipart:
      max-file-size: 10MB
  cloud:
    config:
      discovery:
        service-id: CONFIG
      profile: test